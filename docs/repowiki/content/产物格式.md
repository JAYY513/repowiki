---
status: stable
type: domain
triggers:
  - 产物格式
  - bundle 结构
  - OKF
  - 两族结构
  - 新鲜度
  - 人工保护
description: docs/repowiki/ 产物域：两族目录、页面契约、生成流程与新鲜度机制。
generated: true
source_commit: c0adebe
generator: repowiki-gen
---

# 产物格式

<cite>
**本文引用的文件**
- [skills/repowiki-gen/references/output-spec.md](file://skills/repowiki-gen/references/output-spec.md)
- [bin/repowiki.mjs](file://bin/repowiki.mjs)
- [index.md](../index.md)
</cite>

## 目录

1. [领域模型](#领域模型)
2. [核心概念](#核心概念)
3. [关键流程](#关键流程)
4. [模块边界](#模块边界)
5. [新鲜度与保护](#新鲜度与保护)

## 领域模型

`docs/repowiki/` 是一个 OKF bundle：以 [index.md](../index.md) 为唯一路由入口的 Markdown 页面集，按两族组织（[skills/repowiki-gen/references/output-spec.md:6-28](file://skills/repowiki-gen/references/output-spec.md#L6-L28)）：

```
docs/repowiki/
├── index.md              路由入口：模块清单 + 文章清单（含公共 frontmatter 归集）
├── knowledge/<模块名>.md  一模块一文件（段内 <!-- category:x -->，概述为首段必填）
├── content/              文章（overview / getting_started / domain / deep_dive / developer_guide）+ repo 级收敛卡
├── meta/                 预留：行级引用登记（暂不落盘）
└── log.md                生成日志（finalize 写入）
```

## 核心概念

| 概念 | 含义 |
|---|---|
| 两族 | `knowledge/` 面向「模块」精确命中（段内 category）；`content/` 面向叙事阅读 + repo 级收敛 |
| 段内五维 | 概述 / 架构设计 / 技术栈 / 编码规范 / 特殊配置与命令；`<!-- category:x -->` 段标记，缺段省略 |
| 五类文章 | overview / getting_started / domain / deep_dive / developer_guide，各有章节模板（[skills/repowiki-gen/references/output-spec.md:142-150](file://skills/repowiki-gen/references/output-spec.md#L142-150)） |
| `triggers` | 文章页面的任务意图列表——读取侧命中即加载（route-before-body）；模块文件仅保留 `description`，公共字段归集于 index |
| 引注与图表来源 | 文首 `<cite>` 列引用文件；源码结论带行号锚；Mermaid 图后附图表来源 |

## 关键流程

生成（技能自底向上写作）→ `repowiki validate` 校验（frontmatter、目录约定、链接可达性、plan 一致性，[bin/repowiki.mjs:952-1219](file://bin/repowiki.mjs#L952-L1219)）→ `repowiki state --update` 落基线（页面→源映射 + `content_hash`，[bin/repowiki.mjs:696-769](file://bin/repowiki.mjs#L696-L769)）。

## 模块边界

- 两族职责不重叠：模块文件回答「某模块的知识」（段内 category 直达），文章写叙事与跨模块综合；同一知识不在两族重复（[skills/repowiki-gen/references/output-spec.md:26-28](file://skills/repowiki-gen/references/output-spec.md#L26-L28)）。
- `meta/` 为行级引用登记预留，当前不创建空目录；`index.md` / `log.md` 为保留名。

## 新鲜度与保护

- 基线：`state.json` 记录 git 基线与每页 `content_hash`；`repowiki status` 三态 fresh（0）/ stale（10）/ missing（11），`stale` 时输出 `affected_pages` 驱动增量重生成（[bin/repowiki.mjs:904-918](file://bin/repowiki.mjs#L904-L918)）。
- 人工保护：生成前逐页比对 `content_hash`，手改页面默认跳过；`protected: true` 永久锁定（[skills/repowiki-gen/SKILL.md:185-190](file://skills/repowiki-gen/SKILL.md#L185-L190)）。
- 断点续跑：分批检查点写入 `run.json`，finalize 成功后删除（[skills/repowiki-gen/SKILL.md:204-208](file://skills/repowiki-gen/SKILL.md#L204-L208)）。
